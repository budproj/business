export enum CONSTRAINT {
  ANY = 'any',
  COMPANY = 'company',
  TEAM = 'team',
  OWNS = 'owns',
}

export enum TIMEFRAME_SCOPE {
  CURRENT = 'CURRENT',
  SNAPSHOT = 'SNAPSHOT',
}

export enum DOMAIN_QUERY_ORDER {
  DESC = 'DESC',
  ASC = 'ASC',
}
